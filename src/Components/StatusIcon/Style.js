import { styled } from "@mui/material";
import ArrowCircleUpIcon from "@mui/icons-material/ArrowCircleUp";

export const StyledStatusIcon = styled(ArrowCircleUpIcon)(({color}) => ({
    color : color
  }));
